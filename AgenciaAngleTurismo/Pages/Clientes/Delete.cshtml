@page
@model AgenciaTurismo.Pages_Clientes.DeleteModel

@{
    ViewData["Title"] = "Excluir Cliente";
}

<h1 class="page-header">@ViewData["Title"]</h1>

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm">
            <div class="card-header bg-danger text-white">
                <h4 class="mb-0">Atenção!</h4>
            </div>
            <div class="card-body p-4">
                <p class="fs-5">Você tem certeza que deseja excluir este cliente?</p>
                <p>Esta ação é referente à **exclusão lógica** e marcará o registro como inativo.</p>
                <hr />

                <dl class="row">
                    <dt class="col-sm-3 fw-bold">
                        @Html.DisplayNameFor(model => model.Cliente.Nome)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.Cliente.Nome)
                    </dd>

                    <dt class="col-sm-3 fw-bold">
                        @Html.DisplayNameFor(model => model.Cliente.Email)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.Cliente.Email)
                    </dd>

                    <dt class="col-sm-3 fw-bold">
                        @Html.DisplayNameFor(model => model.Cliente.DataNascimento)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.Cliente.DataNascimento)
                    </dd>
                </dl>

                <form method="post" class="mt-4">
                    <input type="hidden" asp-for="Cliente.Id" />
                    <button type="submit" class="btn btn-danger">Confirmar Exclusão</button>
                    <a asp-page="./Index" class="btn btn-secondary">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>