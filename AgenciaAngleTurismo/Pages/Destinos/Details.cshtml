@page
@model AgenciaTurismo.Pages_Destinos.DetailsModel

@{
    ViewData["Title"] = "Detalhes do Destino";
}

<h1 class="page-header">@ViewData["Title"]</h1>

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm">
            <div class="card-body p-4">
                <dl class="row">
                    <dt class="col-sm-3 fw-bold">
                        @Html.DisplayNameFor(model => model.Destino.Nome)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.Destino.Nome)
                    </dd>

                    <dt class="col-sm-3 fw-bold">
                        @Html.DisplayNameFor(model => model.Destino.Pais)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.Destino.Pais)
                    </dd>
                </dl>

                <div class="mt-4">
                    <a asp-page="./Edit" asp-route-id="@Model.Destino.Id" class="btn btn-primary">Editar</a>
                    <a asp-page="./Index" class="btn btn-secondary">Voltar para a Lista</a>
                </div>
            </div>
        </div>
    </div>
</div>
