@page
@model AgenciaTurismo.Pages_Destinos.DeleteModel

@{
    ViewData["Title"] = "Excluir Destino";
}

<h1 class="page-header">@ViewData["Title"]</h1>

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm">
            <div class="card-header bg-danger text-white">
                <h4 class="mb-0">Atenção!</h4>
            </div>
            <div class="card-body p-4">
                <p class="fs-5">Você tem certeza que deseja excluir permanentemente este destino?</p>
                <hr />

                <dl class="row">
                    <dt class="col-sm-3 fw-bold">
                        @Html.DisplayNameFor(model => model.Destino.Nome)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.Destino.Nome)
                    </dd>
                    <dt class="col-sm-3 fw-bold">
                        @Html.DisplayNameFor(model => model.Destino.Pais)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.Destino.Pais)
                    </dd>
                </dl>

                <form method="post" class="mt-4">
                    <input type="hidden" asp-for="Destino.Id" />
                    <button type="submit" class="btn btn-danger">Confirmar Exclusão</button>
                    <a asp-page="./Index" class="btn btn-secondary">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>

